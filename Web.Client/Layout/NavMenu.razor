@using Web.Client.Models

<nav class="navbar is-dark" role="navigation" aria-label="main navigation">
    <div class="navbar-brand">
        <a role="button" class="navbar-burger @(_collapseNavMenu ? "" : "is-active")" aria-label="menu"
           aria-expanded="false" data-target="navbarBasicExample" @onclick="ToggleNavMenu">
            <span aria-hidden="true"></span>
            <span aria-hidden="true"></span>
            <span aria-hidden="true"></span>
            <span aria-hidden="true"></span>
        </a>
    </div>

    <div class="navbar-menu @(_collapseNavMenu ? "" : "is-active")">
        <div class="navbar-start">
            <NavLink class="navbar-item" href="" Match="NavLinkMatch.All">
                <span>Character</span>
            </NavLink>
            <NavLink class="navbar-item" href="counter">
                <span>Quests</span>
            </NavLink>
        </div>
    </div>
</nav>

@code {
    
    private bool _collapseNavMenu = true; // Controls the burger menu state

    private void ToggleNavMenu()
    {
        _collapseNavMenu = !_collapseNavMenu;
    }

}